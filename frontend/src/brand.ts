/**
 * Nome e simbolo do app em um lugar so.
 *
 * Antes o texto "Fin" estava repetido na sidebar, no login e no titulo da aba;
 * trocar a marca exigia lembrar dos tres. O `<title>` do index.html continua
 * escrito a mao, porque e o que aparece antes do JavaScript carregar.
 */
export const APP_NAME = 'La Casa Del Money'

/** O cifrao e a marca: usado no bloco do logo, sempre com aria-hidden. */
export const APP_SYMBOL = '$'
